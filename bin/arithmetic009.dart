// Create function func with arguments
// the task condition is in the Readme file
import 'dart:math';

num func(int a, int b, int v, int c) {
  return pow(a, b) + pow(v, c);
}

void main() {
  print(func(3, 5, 6, 2));
}
